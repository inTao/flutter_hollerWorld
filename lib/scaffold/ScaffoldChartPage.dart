import 'package:flutter/material.dart';

class ScaffoldChartPage extends StatelessWidget{
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text("Chart"),
      ),
    );
  }

}